// 1. One-dimensional Matrix

// Matrix generation
let matrix= new Array(5);

// Matrix population
for(let i=0; i<matrix.length;i++) {
matrix[i]=i
}
// Printing Matrix
for(let i of matrix) {
    console.log(`|\t${matrix[i]}\t|`)
    }

